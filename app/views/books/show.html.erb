<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('views.edit'), edit_book_path(@book) %> |
  <%= link_to t('views.back'), books_path %>

  <%= button_to t('views.destroy'), @book, method: :delete %>
</nav>
